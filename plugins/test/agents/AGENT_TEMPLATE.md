# {Agent 名称}

> 本文件为 Agent 定义模板。创建新 Agent 时复制此文件并填充各节内容。
> 模板规范见 [CONVENTIONS.md](../CONVENTIONS.md#agent-定义规范)。

## 角色定义

{一句话描述该 Agent 的角色和核心职责}

## 模型

{Opus | Sonnet | Haiku}

选择依据见 [CONVENTIONS.md 模型分层策略](../CONVENTIONS.md#模型分层策略)。

## 执行时机

**{总是启动 | 条件性启动}**：{与谁并行执行 | 启动条件描述}

## 分析重点

### 1. {维度一}
- {检查点 A}
- {检查点 B}

### 2. {维度二}
- {检查点 C}

## 输入

1. **{输入一}**：{格式和来源描述}
2. **{输入二}**：{格式和来源描述}

## 输出格式

```json
{
  "agent": "{agent-id}",
  "findings": [
    {
      "id": "{编号}",
      "description": "{发现描述}",
      "confidence": 85,
      "evidence": "{证据来源：文档原文/代码片段/推理依据}",
      "category": "{分类}"
    }
  ]
}
```

> **例外**：追溯类 Agent（如 forward-tracer、reverse-tracer）使用领域特定输出格式
> （`requirement_to_code` / `code_to_requirement`），不强制遵循通用 `{agent, findings}` 结构。
> 领域特定格式需在 Agent 定义文件中完整描述。

## 置信度评分指南

- **90-100**：{明确支持 — 需求/代码/文档中有直接证据}
- **70-89**：{隐式支持 — 可从上下文合理推断}
- **50-69**：{推测性 — 基于领域知识的合理猜测，但无直接证据}
- **<50**：{不应报告 — 过度推测，不纳入输出}

## 冗余机制

> 仅适用于冗余对 Agent。无冗余对的 Agent 删除此节。

- 与 **{对偶 agent 名称}** 并行独立执行
- 如果两个 Agent 都发现同一问题 → 置信度 +20（封顶 100）
- 独立分析，不共享中间结果

## 注意事项

1. {注意事项一}
2. {注意事项二}
